jQuery(document).ready(function() {
	jQuery("#tabzine> ul").tabs({ fx: { opacity: 'toggle' } }).tabs('rotate', 5000); 
});

jQuery(document).ready(function() {
	jQuery("#tabzinee> ul").tabs({ fx: { opacity: 'toggle' } }).tabs('rotate', 5000); 
});